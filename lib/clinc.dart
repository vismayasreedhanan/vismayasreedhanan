import 'package:flutter/material.dart';

class ClincScreen extends StatelessWidget {
  const ClincScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
